{
  "name": "aicanonfeed",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CANON experiment: AI editor under community rules — same edition for everyone, no filter bubble.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "governance": "node scripts/governance.mjs",
    "pipeline": "node scripts/pipeline.mjs",
    "build-pages": "node scripts/build-pages.mjs",
    "test": "node --test test/constants-hash.test.mjs"
  },
  "dependencies": {
    "rss-parser": "^3.13.0"
  }
}
